# Two app slots plus otadata: the minimum for OTA with rollback.
#
# Sizes assume a 4MB flash. `app0`/`app1` must be identical in size and large
# enough for the biggest image you will ever ship — an image that outgrows its
# slot fails at write time, on the device, in the field.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x6000,
otadata,    data, ota,      0xf000,   0x2000,
phy_init,   data, phy,      0x11000,  0x1000,
ota_0,      app,  ota_0,    0x20000,  0x1E0000,
ota_1,      app,  ota_1,    0x200000, 0x1E0000,
# Device identity. Keep certificates here rather than in the image, so a fleet
# does not share one identity. Encrypt it in production.
certs,      data, nvs,      0x3E0000, 0x10000,
